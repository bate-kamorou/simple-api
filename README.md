# Simple FastAPI Project

A basic REST API built using FastAPI.

## Run the server

``` bash
    pip install -r requirements.txt
    uvicorn app:app --reload
```

## Endpoints

GET /
Returns a Hello World message.
